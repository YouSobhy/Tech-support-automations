# Tech-support-automations

A minimal tech-support chat agent is included and can run locally from the command line.

## Run the chat agent

```bash
python3 chat_agent.py
```

Type your issue and the agent will respond with basic troubleshooting guidance.
Use `exit` to stop the agent.
